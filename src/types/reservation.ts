export type PriceInfoData = {
  성인: {
    count: number;
    totalPrice: number;
  };
  아동: {
    count: number;
    totalPrice: number;
  };
  유아: {
    count: number;
    totalPrice: number;
  };
  totalPay: number;
};
